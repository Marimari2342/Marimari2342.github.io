/**
 * VARIABLES: No podemos declarar variables que empiecen con NUMEROS,
 *            CARACTERES ESPECIALES o PALABRAS RESERVADAS.
 *            Se puede empezar con una LETRA (may o min), con "_" o "$".
 */
var miVariable;     //variable
let miVariable;     //variable más usado, modo nuevo
const IVA=21;       //constante


// COMENTARIOS de una sóla linea

/**
 * COMENTARIOS
 * de muchas
 * lineas hago así.
 */